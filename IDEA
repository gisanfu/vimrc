# update: 2011-08-16

http://tech.16c.cn/linux/top/20100816/49723.html

只要輸入以下的指令，就可以把內容送到clipboard剪貼簿裡面

cat filename | xsel -b -i

filename是文字檔

在vim裡面，指令模式中，使用以下的指令：

"+p

就可以把文字檔裡面的內容，貼到vim裡面

# update: 2011-01-27

當我在按F5的時候，看要不要順利存檔，

然後切換到firefox的時候，看要不要切換後在存檔，不然有些狀況會叫你按任何鍵繼續

# update: 2011-01-03

想要加上切換到firefox的快速鍵，但這個快速鍵是不會做重整動作的，可能放在F7的位置

# update: 2010-11-25

想要加一個快速鍵，會自動切換到firefox browser，並且按下Ctrl+r，
這樣子寫程式就可以減少按快速鍵的數量。

請參考study/window.txt

# update: 2010-11-14

想做兩個腳踏板，可以設定按鍵在各個腳上，

例如右腳是F2，左腳是F4
