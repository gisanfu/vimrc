# update: 2011-01-03

想要把切換的程式，加上引數，可以選擇切換後，要不要按下重整的按鈕。

# update: 2010-11-25

想要加一個快速鍵，會自動切換到firefox browser，並且按下Ctrl+r，
這樣子寫程式就可以減少按快速鍵的數量。

請參考study/window.txt

# update: 2010-11-14

想做兩個腳踏板，可以設定按鍵在各個腳上，

例如右腳是F2，左腳是F4
